[project]
name = "wrong-embedding-overlap-tune-sine-transformers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "sentence-transformers>=5.2.2",
    "uvicorn>=0.40.0",
]
